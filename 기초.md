### CORS

- **서버가 아니라 브라우저가 거는 안전장치.** 서버는 답을 이미 보냈고, 브라우저가 그 답을 JS에게 안 넘겨주는 것.
- 기준은 **출처(origin) = 프로토콜 + 주소 + 포트**. 셋 중 하나만 달라도 다른 출처.
- 서버끼리 통신할 땐 CORS가 아예 없다. 브라우저가 없으니까.
- 우리 프로젝트는 `fetch("/api/messages")` 처럼 **상대경로**를 써서 같은 출처가 됨 → 애초에 안 걸림.

### 프록시 vs 리버스 프록시

차이는 **누구 편에 서느냐** 하나뿐이다.

| | 대신하는 대상 | 모르는 쪽 |
|---|---|---|
| 포워드 프록시 | 손님 | 서버가 진짜 손님을 모름 |
| **리버스 프록시** (우리 nginx) | 가게 | **손님이 진짜 서버를 모름** |

아파트 경비실 = 리버스 프록시. 택배기사는 경비실에만 맡기고 몇 호로 가는지 모른다.
`reverse` 는 "방향이 반대"라는 뜻일 뿐.

### nginx location 우선순위 ★

**위에서부터 순서대로 읽는 게 아니다.**

1. `=` 정확히 일치 → 1순위
2. 그다음 **더 길게 일치하는 쪽**이 이김

그래서 `/api/messages` 는 `/` 가 아니라 `/api/` 에 잡힌다.

```nginx
location /            { try_files $uri $uri/ /index.html; }  # 정적 파일
location /api/        { proxy_pass http://backend-service:8080; }  # 백엔드로 넘김
location = /healthz   { return 200 "OK"; }  # probe 전용
```

### Endpoints

- Service(대표번호)에 **지금 연결돼 있는 Pod 명단**.
- 라벨(`app=backend`)이 맞는 Pod를 쿠버네티스가 자동으로 명단에 올린다.
- **핵심: 준비된(Ready) Pod만 명단에 오른다.** 준비 안 된 Pod로는 요청이 아예 안 간다.
- 확인: `kubectl get endpoints backend-service -n webapp`

### Probe — 이름 말고 "실패했을 때 벌어지는 일"로 외우기

| | readinessProbe | livenessProbe |
|---|---|---|
| 묻는 말 | "손님 받아도 돼?" | "너 살아 있어?" |
| 실패하면 | **명단에서 잠깐 뺀다** (안 죽임) | **껐다 켠다** |

잠깐 바쁜 것뿐인데 재시작하면 오히려 손해 → 그래서 둘로 나눠 놓은 것.

### initContainer

본 컨테이너보다 **먼저 돌고, 끝나야 다음으로 넘어가는** 준비용 컨테이너.
가게 문 열기 전 재료 손질 같은 것. 우리는 여기서 `npm install` 을 한다.
